name: CI
on:
    push:
    merge_group:
    pull_request:
jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: software-mansion/setup-scarb@v1
              with:
                  scarb-version: "0.6.2"
            - run: for dir in ./examples/* ; do cd $dir && scarb fmt --check && cd ../..; done;
            - run: curl -L https://install.dojoengine.org | bash && dojoup

